# Generate testing data for cassandra

```bash
./cas-replica-testing-osx --help                                                                                                                                                            130 ↵
Usage of ./cas-replica-testing-osx:
  -enableAuth
        enable auth, pls use with username, password flags
  -finishAt int
        test records finish number (default 1000000)
  -keyspace string
        keyspace name (default "example")
  -password string
        Password for auth (default "password")
  -server string
        ex: 192.168.1.1 (default "127.0.0.1")
  -startAt int
        test records start number (default 1)
  -username string
        Username for auth (default "user")
```